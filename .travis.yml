language: java

jobs:
  include:
    - stage: sonar
      name: Test 1
      script: bash scripts/sonar.sh

    - stage: docker
      name: Test 2
      script: bash scripts/docker.sh

branches:
  only:
    - develop

cache:
  directories:
    - $HOME/.m2

services:
  - docker

addons:
  sonarcloud:
    organization: "nzolo0"
    token: "$SONARCLOUD_TOKEN"
